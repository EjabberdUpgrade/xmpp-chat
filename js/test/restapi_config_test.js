var assert = require('assert'),
    fs = require('fs'),
    path = require('path'),
    testCase = require('nodeunit').testCase;
    Sys = require('sys');

var ejabConf = path.join(__dirname, '/js/test/restapi_config.json');
